#!/usr/bin/env bash
# Format all Swift sources in place using swift-format (ships with Xcode).
source "$(dirname "$0")/lib.sh"

xcrun swift-format format --in-place --recursive ZenTab ZenTabTests
echo "Formatted ZenTab and ZenTabTests."
