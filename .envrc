# direnv: put the macOS app's bin/ scripts on PATH so `run`, `run-prod`, `build`,
# `test`, `format`, `lint`, `generate`, etc. work from anywhere in the repo.
# PATH_add resolves relative to this .envrc and prepends, so these win over any
# same-named tools. Run `direnv allow` once to enable.
PATH_add darwin/bin
