# Root-level ignores for the ZenTab monorepo.
# Per-platform ignores live in darwin/.gitignore and windows/.gitignore.

# macOS
.DS_Store

# Editor / OS junk
*.swp

# direnv local cache (.envrc itself IS committed)
.direnv/
